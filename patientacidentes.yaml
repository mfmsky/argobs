apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-acidentes
spec:
  selector:
    matchLabels:
      app: backend-acidentes
  template:
    metadata:
      labels:
        app: backend-acidentes
    spec:
      imagePullSecrets:
      - name: secretpat
      containers:
      - name: backend-acidentes
        image: pmdferreira/patient-care:acidentesv3
        resources:
          limits:
            memory: "500Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: acidentes-service
spec:
  selector:
    app: backend-acidentes
  ports:
  - port: 7896
    targetPort: 80

        
