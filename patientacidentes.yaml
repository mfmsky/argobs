apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-acidentes
spec:
  selector:
    matchLabels:
      app: backend-acidentes
  template:
    metadata:
      labels:
        app: backend-acidentes
    spec:
      imagePullSecrets:
      - name: secretpat
      containers:
      - name: backend-acidentes
        image: acidentesv1/acidentesv1
        resources:
          limits:
            memory: "500Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
        
